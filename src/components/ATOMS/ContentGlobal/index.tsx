import { ReactNode } from "react";
import * as Styles from "./styles";

export function ContentGlobal({ children }: { children: ReactNode }) {
  return <Styles.ContentGlobal>{children}</Styles.ContentGlobal>;
}
